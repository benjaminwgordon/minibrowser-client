import React from "react";

// renders a list of RecipeStep Components representing each step necessary to recreate the model in a Post
const Recipe = () => {
  return (
    <div>
      <div></div>
    </div>
  );
};

export default Recipe;
